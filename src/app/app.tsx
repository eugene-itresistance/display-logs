// Modules
import type { FC } from 'react';

// Components
import { LogViewComponent } from '../components/log-view';

export const App: FC = () => {
  return (
    <LogViewComponent />
  );
};

export default App;